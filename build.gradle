// Top-level build file where you can add configuration options common to all sub-projects/modules.
plugins {
    // 这里必须声明版本号，Gradle 8.0 对应 AGP 8.0.0 或更高
    id 'com.android.application' version '8.0.0' apply false
}
